{
  "name": "Demo",
  "version": "0.0.1",
  "description": "Yuying Wu's Demo",
  "author": "Yuying Wu",
  "dependencies": {
    "fis3-hook-module": "*",
    "fis3-postpackager-loader": "*",
    "fis-parser-less": "*"
  },
  "scripts": {
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -w -r 0"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.0.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
